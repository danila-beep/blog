specVersion: "v1"
serviceId: "blog-project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "blog-project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
